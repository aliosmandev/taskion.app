services:
  web:
    build:
      context: .
      dockerfile: ./apps/landing/Dockerfile
    restart: always
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.postiva.entrypoints=websecure"
      - "traefik.http.routers.postiva.tls.certResolver=letsencrypt"
      - "traefik.http.routers.postiva.rule=Host(`taskion.app`)"
      - "traefik.http.services.postiva.loadbalancer.server.port=7000"
    ports:
      - 7000:7000

networks:
  dokploy-network:
    external: true
